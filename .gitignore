# dependencies
node_modules/

# build output
dist/

# fetched + generated data — regenerate with:
#   ./scripts/get-latest-combined-data.sh   (-> data-combined/)
#   npm run transform                        (-> public/data/)
/data-combined/
/public/data/

# release assets pulled by history.mjs while building windows.
# The finalized windows in data-history/ are the durable artefact.
/data-releases/

# local env
.env
.env.*
!.env.example

# os / editor / logs
.DS_Store
*.log
.vite/
